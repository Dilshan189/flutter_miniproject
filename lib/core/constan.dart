const double kDouble5 = 5.0;
const double kDouble10 = 10.0;
const double kDouble20 = 20.0;
const double kDouble40 = 40.0;


const String beconDescription = 'Becon You can inset the start of the paragraph with spaces. Though this may not be consistent from while loop will need to temporarily make the first word appear larger exercise would be to add a setting to the settings that indents the first line by a fixed amount This example shows how to render text along an arc Becon You can inset the start of the paragraph with spaces. Though this may not be consistent from while loop will need to temporarily make the first word appear larger exercise would be to add a setting to the settings that indents the first line by a fixed amount This example shows how to render text along an arc ';

